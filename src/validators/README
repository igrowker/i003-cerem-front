En esta carpeta se encuentran los validadores, que contienen la lógica de validación de datos. Esto puede incluir validación de formularios, estructuras de datos o cualquier otra verificación necesaria antes de enviar datos a un servidor o guardarlos en el estado.